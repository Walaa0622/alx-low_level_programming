#include <stdio.h>
#include <stdlib>
/**
 * main - Entry point,prints the alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char c;
	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'Z'); {
			putchar(d);
			d++;
		}
	putchar('\n'0;
	return (0);
}
